
[workspace]
resolver = "3"
members = [
    "genmeta",
    "genmeta-access",
    "genmeta-common",
    "genmeta-curl",
    "genmeta-ssh",
    "genmeta-nslookup",
    "genmeta-discover",
    "genmeta-nat",
    "genmeta-doctor",
    "genmeta-identity",
    "genmeta-proxy",
    "xtask",
]

[workspace.dependencies]
# async
futures = "0.3"
daemonize = "0.5"
tokio = { version = "1", features = ["full"] }

# cli
clap = { version = "4", features = ["derive"] }
inquire = "0.9"
inquire-derive = "0.9"
indicatif = "0.18"
rpassword = "7.3"
whoami = "2"

# debug
console-subscriber = "0.5"

# network
gmdns = { git = "ssh://git@github.com/genmeta/gmdns.git", branch = "main" }
h3x = { git = "https://github.com/genmeta/h3x.git", tag = "v0.2.0" }
http = "1"
hyper = { version = "1", features = ["http1", "server"] }
hyper-util = { version = "0.1", features = ["server", "tokio"] }
http-body-util = "0.1"
reqwest = { version = "0.13", default-features = false, features = [
    "charset",
    "http2",
    "system-proxy",
    "gzip",
    "json",
    "zstd",
    "rustls-no-provider",
] }

# security
rankey = { git = "ssh://git@github.com/genmeta/rankey.git", branch = "master" }
rustls = { version = "0.23", default-features = false, features = ["ring"] }
x509-parser = "0.14"

# serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_cbor = "0.11"
toml = "1"

# tracing
tracing = { version = "0.1", default-features = false, features = [
    "std",
    "attributes",
] }
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", default-features = false, features = [
    "env-filter",
    "fmt",
    "ansi",
    "time",
    "local-time",
] }
tracing-indicatif = { version = "0.3" }

# builder
bon = "3"

# utils
base64 = "0.22"
bytes = "1"
crossterm = { version = "0.29" }
dashmap = { version = "6" }
derive_more = { version = "2" }
dirs = { version = "6" }
either = "1"
globset = "0.4"
peg = "0.8"
sea-orm = { version = "1", features = ["runtime-tokio-rustls"] }
sea-orm-migration = { version = "1", features = ["runtime-tokio-rustls"] }
snafu = "0.9"
socket2 = "0.5"
tokio-util = { version = "0.7" }
async-compression = { version = "0.4", features = [
    "tokio",
    "gzip",
    "deflate",
    "zstd",
] }
trust-dns-resolver = "0.23.2"

# workspace
firewall-base = { git = "ssh://git@github.com/genmeta/firewall.git", branch = "main" }
firewall-db = { git = "ssh://git@github.com/genmeta/firewall.git", branch = "main" }
firewall-migration = { git = "ssh://git@github.com/genmeta/firewall.git", branch = "main" }
genmeta-access = { path = "genmeta-access" }
genmeta-common = { path = "genmeta-common" }
dhttp-home = { git = "ssh://git@github.com/genmeta/dhttp-home.git", branch = "main" }
genmeta-curl = { path = "genmeta-curl" }
genmeta-discover = { path = "genmeta-discover" }
genmeta-doctor = { path = "genmeta-doctor" }
genmeta-identity = { path = "genmeta-identity" }
genmeta-nat = { path = "genmeta-nat" }
genmeta-nslookup = { path = "genmeta-nslookup" }
genmeta-ssh = { path = "genmeta-ssh" }
genmeta-proxy = { path = "genmeta-proxy" }

# ssh
genmeta-ssh-core = { package = "genmeta-ssh", git = "ssh://git@github.com/genmeta/genmeta-ssh.git", branch = "main", features = [
    "config",
] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

[patch."ssh://git@github.com/genmeta/h3x.git"]
h3x = { path = "../h3x" }

# Local development overrides: point h3x and gmdns at the in-tree checkouts
# so this workspace consumes the unreleased endpoint-API changes.
[patch."https://github.com/genmeta/h3x.git"]
h3x = { path = "../h3x" }

[patch."ssh://git@github.com/genmeta/gmdns.git"]
gmdns = { path = "../gmdns" }

[patch.crates-io]
# TEMP: use local dquic while h3x's endpoint API consumes unreleased dquic
# accessors (matches h3x's own Cargo.toml dev-time override).
dquic = { path = "../dquic/dquic" }
