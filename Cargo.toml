[workspace]
resolver = "3"
members = [
    "genmeta",
    "genmeta-common",
    "genmeta-curl",
    "genmeta-ssh3",
    "genmeta-nslookup",
    "genmeta-discover",
    "genmeta-nat",
    "ssh3-proto",
    "ssh-config",
    "genmeta-profile",
    "genmeta-doctor",
]

[workspace.dependencies]
# async
async-trait = "0.1"
tokio = { version = "1", features = ["full"] }
futures = "0.3"

# cli
clap = { version = "4", features = ["derive"] }
rpassword = "7.3"
whoami = "1.4"

# genmeta
gm-quic = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }
gmdns = { git = "ssh://git@github.com/genmeta/gmdns.git", branch = "main" }
h3-shim = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }
qbase = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }
qdns = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }
qevent = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }
qinterface = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }
qtraversal = { git = "ssh://git@github.com/genmeta/gm-quic-traversal.git", branch = "feat/v0.3" }


# http
http = "1"
h3 = { version = "0.0.8", features = ["tracing"] }

# security
rustls = { version = "0.23", default-features = false, features = ["ring"] }
# dont load system certs
# rustls-native-certs = "0.8" 

# serialization
serde = { version = "1", features = ["derive"] }
serde_cbor = "0.11"

# tracing
tracing = { version = "0.1", default-features = false, features = [
    "std",
    "attributes",
] }
tracing-subscriber = { version = "0.3", default-features = false, features = [
    "env-filter",
    "fmt",
    "ansi",
    "time",
    "local-time",
] }
console-subscriber = "0.4"

# utils
atty = "0.2"
bytes = "1"
base64 = "0.22"
crossterm = { version = "0.29" }
dashmap = { version = "6" }
dirs = { version = "6" }
derive_more = { version = "2" }
either = { version = "1" }
peg = "0.8"
pin-project-lite = "0.2"
regex = "1"
regex-syntax = "0.8"             # escape sequences
snafu = "0.8"
socks5-proto = "0.4"
socket2 = "0.5"
trust-dns-resolver = "0.23.2"
thiserror = "2"
tokio-util = { version = "0.7" }

# workspace
genmeta-common = { path = "genmeta-common" }     # common code
ssh3-proto = { path = "ssh3-proto" }             # ssh3-common
ssh-config = { path = "ssh-config" }             # ssh-like config parser
genmeta-ssh3 = { path = "genmeta-ssh3" }         # ssh3 
genmeta-curl = { path = "genmeta-curl" }         # curl
genmeta-nslookup = { path = "genmeta-nslookup" } # nslookup
genmeta-discover = { path = "genmeta-discover" } # discover
genmeta-doctor = { path = "genmeta-doctor" }     # doctor
genmeta-nat = { path = "genmeta-nat" }           # nat

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
